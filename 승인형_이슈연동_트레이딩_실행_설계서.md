# 승인형 이슈연동 트레이딩 실행 설계서

## 1. 목적
이 설계의 목표는 다음 3가지를 동시에 만족하는 것입니다.
1. 뉴스 이벤트를 5~10분 단위로 빠르게 감지한다.
2. LLM 호출 비용을 통제한다.
3. 자동매매 리스크를 줄이기 위해 사용자 승인 후에만 주문을 실행한다.

## 2. 현재 문제
1. 실시간 감지는 가능하지만, 고비용 모델 토론을 자주 돌리면 비용이 급증한다.
2. 반대로 뉴스 체크를 하루 1~2회로 줄이면 단기 대응력이 떨어진다.
3. 단기 대응과 비용 통제를 동시에 만족하려면 `수집`과 `판단`과 `실행`을 분리해야 한다.

## 3. 핵심 아키텍처
1. Layer A: 실시간 수집(저비용)
- `run_news.bat` 10분 주기 실행
- 기사 정규화, 중복 제거, 이벤트 클러스터링, 체크포인트 저장
- LLM 호출 없음

2. Layer B: 1차 트리아지(규칙 기반)
- 이벤트별 영향점수 계산
- 점수 요소: 보유종목 연관성, 시가총액 영향, 키워드 강도(가이던스/리콜/소송/규제/수주/제재)
- 점수가 낮으면 저장만 하고 종료

3. Layer C: 2차 트리아지(신규 이벤트 웹검증)
- Layer B를 통과한 **신규 이벤트만** 웹검색 증거 패키지 생성
- 근거 수와 도메인 다양성(최소 2개 이상)으로 검증 충분/부족 판정
- 방향성은 DB 요약문이 아닌 웹검증 결과로 보정

4. Layer D: 고비용 심층 검증(선택적)
- 다음 중 하나일 때만 실행
- 사용자 보유 포트폴리오 직접 타격
- 영향점수 상위 임계치 초과
- 로컬 모델 신뢰도 불확실
- 이때만 SEARCH + 멀티 모델 토론 수행

5. Layer E: 승인형 주문 실행
- 봇이 주문 제안 생성
- 사용자 승인(`!승인`) 후에만 브로커 주문
- 승인 없으면 자동 만료

## 4. 디스코드 명령어 제안
1. `!시그널`
- 최근 감지된 이벤트 시그널 목록 요약

2. `!시그널상세 [EVENT_ID]`
- 해당 이벤트의 근거, 점수, 제안 주문안 조회

3. `!승인 [EVENT_ID]`
- 미체결 승인대기 주문 실행

4. `!거부 [EVENT_ID]`
- 승인대기 주문 폐기

5. `!승인목록`
- 현재 승인대기/만료 임박 항목 목록

6. `!자동매매중지`
- 주문 실행 회로 차단기 즉시 ON

## 5. 데이터 모델 제안
1. `signal_events`
- `event_id`, `event_key`, `detected_at`, `score_total`, `score_breakdown_json`, `status`

2. `signal_recommendations`
- `event_id`, `ticker`, `side`, `size_rule`, `entry_rule`, `stop_rule`, `ttl_sec`, `confidence`

3. `approval_requests`
- `event_id`, `requested_at`, `expires_at`, `approved_by`, `approved_at`, `state`

4. `order_executions`
- `event_id`, `broker_order_id`, `submitted_at`, `filled_at`, `avg_price`, `qty`, `result`

5. `risk_guardrail_state`
- `daily_loss_limit`, `daily_order_limit`, `hourly_order_limit`, `cooldown_until`, `kill_switch`

## 6. 주문 실행 가드레일
1. 기본값은 `paper trading`
2. 실거래 전환 후에도 1일 손실 한도 초과 시 자동 중지
3. 동일 이벤트 재주문 쿨다운 적용
4. 장외/유동성 부족 구간 주문 금지
5. 주문 제안 TTL(예: 15분) 만료 시 자동 폐기
6. 브로커 오류/체결 실패 누적 시 회로 차단기 ON

## 7. 비용 통제 정책
1. 10분 수집 루프는 무LLM
2. 로컬 모델 요약은 이벤트 임계치 통과 건만 실행
3. 프론티어 모델 토론은 `영향점수 상위 + 포지션 직접 영향` 조건에서만 실행
4. 일일 토큰 예산 초과 시 자동으로 로컬 전용 모드 강등

## 8. 알림 포맷 표준
1. 이벤트 식별: `EVENT_ID`, 발생시각, 관련티커
2. 근거: 핵심 링크 2~3개, 근거ID 목록
3. 제안: 매수/매도, 수량 규칙, 손절/무효화 조건
4. 리스크: 최악 시나리오 손실 추정, 변동성 경고
5. 액션: `!승인 EVENT_ID` 또는 `!거부 EVENT_ID`

## 9. 운영 플로우
1. 뉴스 수집 배치가 이벤트 저장
2. 규칙 기반 점수화
3. 임계치 통과 + 신규 이벤트이면 웹검색 증거 패키지 검증
4. 필요 시 심층 검증
5. 승인 요청 생성
6. 사용자 승인 후 주문 실행
7. 체결 결과 및 사후 성과 기록

## 12. DB 사용 원칙
1. DB는 결론 엔진이 아니라 증거 저장소로 사용
2. 판단은 최신 웹검증 결과를 우선하고, 과거 DB 결론은 참고 정보로만 사용
3. 동일 이벤트 재판정 시에도 최신 증거 패키지가 없으면 승인 대기를 생성하지 않음

## 10. 단계별 구현 계획
1. Phase 1
- 이벤트 점수화 + 승인요청 저장 + 디스코드 조회/승인/거부 명령

2. Phase 2
- Paper trading 엔진 연동 + 체결 시뮬레이션

3. Phase 3
- 실거래 브로커 연동(소액 한도), 킬스위치 적용

4. Phase 4
- 사후성과 리포트와 점수 모델 자동 보정

## 11. 파일 단위 변경 제안
1. `src/main.py`
- `!시그널`, `!승인`, `!거부`, `!승인목록`, `!자동매매중지` 명령 추가

2. `src/db_manager.py`
- 시그널/승인/주문/리스크 테이블 추가

3. `src/debate_manager.py`
- 이벤트 기반 심층 검증 진입 조건 훅 추가

4. `src/trading_executor.py` (신규)
- paper/live 주문 실행 어댑터, 브로커 추상화

5. `src/signal_engine.py` (신규)
- 이벤트 점수화, 트리아지, 승인요청 생성
